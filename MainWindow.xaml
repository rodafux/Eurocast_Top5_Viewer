<Window x:Class="Eurocast_Top5_Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Eurocast_Top5_Viewer.ViewModels"
        Title="Eurocast Top5 Dashboard" 
        WindowStyle="None" WindowState="Maximized" Topmost="True" Background="#1C1C1C">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="TABLEAU DE BORD QUALITÉ" Foreground="#FFFFFF" FontSize="28" FontWeight="Black"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="#4D1919" BorderBrush="#888" BorderThickness="1" Width="20" Height="20" Margin="0,0,10,0"/>
                <TextBlock Text="NC - Non conforme" Foreground="#FFCDD2" FontSize="18" FontWeight="Bold" Margin="0,0,30,0"/>

                <Border Background="#4D3B0A" BorderBrush="#888" BorderThickness="1" Width="20" Height="20" Margin="0,0,10,0"/>
                <TextBlock Text="AA - À améliorer" Foreground="#FFECB3" FontSize="18" FontWeight="Bold"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="#3C3C3C" BorderBrush="#555555" BorderThickness="1" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="180"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="État" Foreground="White" FontSize="18" FontWeight="Bold" TextAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="Date d'apparition" Foreground="White" FontSize="18" FontWeight="Bold" TextAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="Défaut (et Noyau)" Foreground="White" FontSize="18" FontWeight="Bold"/>
                <TextBlock Grid.Column="3" Text="Commentaire" Foreground="White" FontSize="18" FontWeight="Bold"/>
                <TextBlock Grid.Column="4" Text="Temps écoulé" Foreground="White" FontSize="18" FontWeight="Bold" TextAlignment="Center"/>
            </Grid>
        </Border>

        <ListBox Grid.Row="2" ItemsSource="{Binding DashboardItems}" Background="Transparent" BorderThickness="0" 
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                 ScrollViewer.VerticalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="{Binding BackgroundColor}" BorderBrush="#555555" BorderThickness="1,0,1,1">
                        <Grid>
                            <Border Padding="15" Visibility="{Binding IsHeader, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="{Binding MachineInfo}" Foreground="{Binding TextColor}" FontSize="24" FontWeight="Black" HorizontalAlignment="Center"/>
                            </Border>

                            <Grid Margin="15" Visibility="{Binding IsNotHeader, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="3*"/>
                                    <ColumnDefinition Width="180"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="2" BorderBrush="#E0E0E0" BorderThickness="0,0,2,0" Margin="0,0,15,0"/>

                                <TextBlock Grid.Column="0" Text="{Binding State}" Foreground="{Binding TextColor}" FontSize="22" FontWeight="Bold" VerticalAlignment="Center" TextAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding CreationDateStr}" Foreground="{Binding TextColor}" FontSize="18" VerticalAlignment="Center" TextAlignment="Center"/>

                                <TextBlock Grid.Column="2" Text="{Binding DefectDisplay}" Foreground="{Binding TextColor}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" TextWrapping="Wrap" Margin="0,0,25,0"/>

                                <TextBlock Grid.Column="3" Text="{Binding Comment}" Foreground="{Binding TextColor}" FontSize="20" VerticalAlignment="Center" TextWrapping="Wrap" Margin="0,0,15,0"/>

                                <TextBlock Grid.Column="4" Text="{Binding ElapsedTime}" Foreground="{Binding TextColor}" FontSize="22" FontWeight="Bold" VerticalAlignment="Center" TextAlignment="Center"/>
                            </Grid>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="3" Margin="0,15,0,0">
            <TextBlock HorizontalAlignment="Left" Text="{Binding PageIndicatorText}" Foreground="#FFFFFF" FontSize="20" FontWeight="Bold" Background="#333333" Padding="15,5" />

            <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" Text="{Binding LastUpdateText}" Foreground="#888888" FontSize="16" />
        </Grid>
    </Grid>
</Window>